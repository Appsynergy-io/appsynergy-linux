# Packages pacstrap'd onto the machine by appsynergy-install.
# One name per line. Kernel: local igpu pkgs via --kernel=local (default)
# or add linux/linux-headers when --kernel=repo.

# --- base / boot / disk ---
base
base-devel
linux-firmware
sof-firmware
intel-ucode
cryptsetup
tpm2-tss
tpm2-tools
btrfs-progs
dosfstools
e2fsprogs
exfatprogs
ntfs-3g
efibootmgr
mkinitcpio

# --- network / security ---
networkmanager
iwd
wireless-regdb
wireguard-tools
bluez
bluez-utils
bluez-obex
bluez-hid2hci
openssh
ufw
nftables

# --- console ---
sudo
which
less
man-db
man-pages
rsync
tar
zstd
unzip
zip
wget
curl
jq
bash-completion
fish
bind
fwupd

# --- GPU / media (Intel only) ---
intel-media-driver
mesa
vulkan-icd-loader
vulkan-intel
pipewire
pipewire-audio
pipewire-alsa
pipewire-pulse
wireplumber
ffmpeg

# --- desktop (Plasma Breeze) ---
sddm
plasma-desktop
plasma-workspace
plasma-nm
plasma-pa
plasma-systemmonitor
plasma-browser-integration
kscreen
kwin
breeze
breeze-gtk
breeze-icons
kde-gtk-config
dolphin
konsole
spectacle
kate
ark
kscreenlocker
# KWallet unlock at SDDM + manager UI + signon backend for KDE accounts
kwallet-pam
kwalletmanager
signon-kwallet-extension
kaccounts-integration
# FIDO2 / security keys + hybrid passkeys (Brave/Chromium + bluez Experimental)
libfido2
xdg-desktop-portal
xdg-desktop-portal-kde
xdg-user-dirs
bluedevil
powerdevil
polkit-kde-agent
btrfs-assistant
snapper
alacritty
fastfetch
noto-fonts
noto-fonts-emoji
ttf-liberation
ttf-dejavu
ttf-meslo-nerd
# Browsers: no Firefox. Brave shipped as local pkg under /opt/appsynergy/pkgs
# (pacman -U at end of install). Thorium is AUR — drop
# thorium-browser-bin-*.pkg.tar.zst into that pkgs dir before build, or:
#   paru -S thorium-browser-bin
# after first boot. Future: custom AppSynergy browser replaces both.

# --- general dev ---
git
github-cli
containerd
nerdctl
cni-plugins
rustup
go
nodejs
npm
pnpm
python
python-pip
python-pipx
code
gdb
perf
ripgrep
tmux
btop
vim
nano
lsof
strace
qemu-base

# --- Bazel / appsynergy-rs / NativeLink worker host ---
# .bazelversion pins 8.3.1 — install bazelisk (downloads that version).
# Do NOT pin distro `bazel` 9.x as the only binary; bazelisk honors .bazelversion.
bazelisk
jdk17-openjdk
java-runtime-common
java-environment-common
# Host CC toolchain (worker farm + --config=local). base-devel already
# pulls gcc/binutils/make/pkgconf; list critical names so a partial
# strip cannot drop them.
gcc
binutils
glibc
linux-api-headers
pkgconf
openssl
# C/C++ extras used by crate build scripts (ring, openssl-sys, etc.)
clang
llvm
compiler-rt
lld
cmake
ninja
nasm
perl
# Optional faster linker for cargo path (not required by farm gcc toolchain)
mold
# Proto (some rules / tools on host)
protobuf
# musl release path (scripts/release.sh, static agent)
musl
kernel-headers-musl
# sandbox / FS helpers Bazel and containers use
fuse3
fuse-overlayfs

# --- intentionally NOT here (restore / install later) ---
# nativelink binary → /opt/nativelink from backup (47G tree optional)
# ~/bin/bazel farm wrapper (WoL tether) → restore from home backup
# cuda, mingw-w64-gcc, NVIDIA stack, firefox
#
# NEVER ship:
# - firefox (Brave + Thorium only; own browser later)
# - hyfetch, lolcat, pride wallpapers, cachyos-*-theme*, cachyos-wallpapers
# - archlinux.gay (mirror hostname — stripped by appsynergy-sanitize-mirrors)
# - age-verification apps / browser experiment stubs

# --- AppSynergy identity (NOT pacstrap'd) ---
# Live /etc/pacman.conf is stock Arch (no [appsynergy], conf.d unused).
# appsynergy-install installs these after pacstrap via:
#   1) public repo once [appsynergy]+XferCommand is registered, else
#   2) local /opt/appsynergy/pkgs/appsynergy-{branding,mirrorlist}-*.pkg.tar.zst
# Do not list them here — pacstrap would fail looking in core/extra only.
