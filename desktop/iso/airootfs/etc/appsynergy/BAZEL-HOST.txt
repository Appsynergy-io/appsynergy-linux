AppSynergy Linux — Bazel / NativeLink host notes
================================================

appsynergy-rs
  .bazelversion     8.3.1   (bazelisk downloads this; do not rely on distro bazel 9.x alone)
  MODULE.bazel      rules_rust + rust 1.90.0 hermetic; host gcc for --config=local and farm worker
  Farm default      ~/.bazelrc → grpc://127.0.0.1:50051 (NativeLink)
  Local only        bazel build --config=local //...
  check.sh          prefers $HOME/bin/bazel (WoL wrapper), else PATH bazel

Installed via packages-target.txt
  bazelisk, jdk17-openjdk, gcc, binutils, openssl, pkgconf,
  clang, llvm, lld, compiler-rt, cmake, ninja, nasm, perl, mold,
  protobuf, nodejs, npm, pnpm, go, rustup, musl, kernel-headers-musl,
  docker, fuse-overlayfs, zip, unzip, git, python, …

Restore after install (not in pacstrap)
  ~/bin/bazel              farm wrapper
  ~/.bazelrc               remote_executor / remote_cache flags
  /opt/nativelink/         nativelink binary + config.json5
  systemd nativelink.service

Quick smoke (after restore + network)
  java -version            # 17
  bazelisk version         # pulls 8.3.1 once
  gcc --version
  cd ~/projects/appsynergy-rs && bazel build --config=local //agent:appsynergy-agent
