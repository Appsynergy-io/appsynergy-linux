[package]
name = "appsynergy-install"
version = "0.7.1"
edition = "2021"
description = "AppSynergy unified installer: desktop | server dual-NVMe btrfs RAID1"
license = "MIT"
publish = false

[[bin]]
name = "appsynergy-install"
path = "src/main.rs"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive", "env"] }
# regex not needed — stick to std

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = "s"
